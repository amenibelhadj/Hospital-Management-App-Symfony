{% extends 'base.html.twig' %}
{% block body %}
         <section class="section register min-vh-100 d-flex flex-column align-items-center justify-content-center py-4 text-center">
            <div class="container">
               <div class="row justify-content-center ">
                        <div class="card-body ">
                           <div class="pt-4 pb-2">
                              <h1 class="card-title text-center pb-0 fs-4">Créez votre compte</h1>
                              <h6 class="text-center ">Entrez vos données personnelles</h6>
                           </div>
                           {{ include('user/_form.html.twig') }}
                        </div>
               </div>
            </div>
         </section>
{% endblock %}