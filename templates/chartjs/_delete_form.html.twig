<form method="post" action="{{ path('app_chartjs_delete', {'id': chartj.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chartj.id) }}">
    <button class="btn">Delete</button>
</form>
